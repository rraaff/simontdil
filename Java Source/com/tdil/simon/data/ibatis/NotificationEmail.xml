<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="NotificationEmail">

  <!-- Use type aliases to avoid typing the full classname every time. -->
  <typeAlias alias="NotificationEmail" type="com.tdil.simon.data.model.NotificationEmail"/>

  <select id="selectEmailByKey" parameterClass="java.lang.String" resultClass="NotificationEmail">
    select
      *
    from NOTIFICATIONEMAIL
    where name = #name#
    and deleted = 0
  </select>
  
  <insert id="insertEmail" parameterClass="NotificationEmail">
    insert into NOTIFICATIONEMAIL (
      id, emailKey, emailText, deleted
    ) values (
      #id#, #emailKey#, #emailText#, #deleted#
    )
  </insert>

  <update id="updateEmail" parameterClass="NotificationEmail">
    update NOTIFICATIONEMAIL set
      status = #emailText#
      deleted = #deleted#
    where
      emailKey = #emailKey#
  </update>
  
</sqlMap>